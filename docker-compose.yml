version: '3.8'

services:
  industrial-security-demo:
    build:
      context: .
      dockerfile: Dockerfile
    image: industrial-security-demo:latest
    container_name: industrial-security-demo
    runtime: nvidia
    network_mode: host
    privileged: true
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - QT_QPA_PLATFORM=offscreen
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
      - LD_LIBRARY_PATH=/usr/lib/aarch64-linux-gnu:/usr/local/cuda/lib64:${LD_LIBRARY_PATH:-}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ./config:/app/config:ro
      - ./output:/app/output:rw
      - ./models:/app/models:ro
      - /usr/lib/aarch64-linux-gnu:/usr/lib/aarch64-linux-gnu:ro
      - /usr/local/cuda:/usr/local/cuda:ro
      - /usr/lib/tensorrt:/usr/lib/tensorrt:ro
      - /var/run/dbus:/var/run/dbus:ro
      # TensorRT Python 绑定（JetPack 6.x）
      - /usr/lib/python3/dist-packages/tensorrt:/usr/lib/python3/dist-packages/tensorrt:ro
      - /usr/lib/python3/dist-packages/tensorrt_dispatch:/usr/lib/python3/dist-packages/tensorrt_dispatch:ro
      - /usr/lib/python3/dist-packages/tensorrt_lean:/usr/lib/python3/dist-packages/tensorrt_lean:ro
      - /usr/lib/python3/dist-packages/pycuda:/usr/lib/python3/dist-packages/pycuda:ro
    devices:
      - /dev/nvidia0:/dev/nvidia0
      - /dev/nvidiactl:/dev/nvidiactl
      - /dev/nvmap:/dev/nvmap
      - /dev/nvhost-ctrl:/dev/nvhost-ctrl
      # 摄像头设备（按需取消注释）
      # - /dev/video0:/dev/video0
      # - /dev/video1:/dev/video1
    cap_add:
      - SYS_ADMIN
      - NET_ADMIN
    ipc: host
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s=socket.socket(); s.connect(('localhost', 8080)); s.close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
